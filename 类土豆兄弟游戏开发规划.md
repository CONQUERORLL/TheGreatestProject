# 类土豆兄弟（Brotato）游戏开发完整规划

> 参考原型：《Brotato / 土豆兄弟》——俯视角生存射击 Roguelite，单人开发、Godot 引擎、全球销量超 1000 万份。
> 本文档覆盖：技术选型、核心系统拆解、开发里程碑、美术/音频资源、测试与发布。

---

## 〇、当前进度与决策（2026-09-11 校准）

> 本节数字均以代码为准重新核对：内容量取自 `Registry` 注册表（运行时真值），
> 玩法常量取自 `game/scripts/core/config.gd`（唯一数值源）。
> 冒烟测试 `game/tests/smoke_test.gd` 每次回归都会断言内容量下限与数据自洽。

### 关键决策
| 项 | 决策 |
|------|------|
| **目标平台** | PC（Windows 优先）+ 移动端（Android/iOS）+ 手柄适配 |
| **游戏引擎** | Godot 4.7.2-stable（GL Compatibility 渲染后端，1280×720，canvas_items 拉伸 + keep 宽高比） |
| **项目代码** | 仓库内 `game/` 目录（Godot 项目根），主场景 `res://scenes/ui/main_menu.tscn` |
| **项目代号** | `BrotatoLite`（`project.godot` 的 `config/name`） |
| **规则集版本** | `Config.RULESET_ID = "brotato-lite-v1.0"` |
| **题材** | 中式玄幻：五行状态 / 门派角色 / 法宝 / 水墨主题关卡 |
| **Web 原型定位** | 玩法/数值验证沙盒，保留用于快速试数值 |

### 玩法内容量速查（代码真值）

| 类别 | 实际数量 | 说明 |
|------|---------|------|
| 角色 | **12** | 基础 6（土豆勇者/狂战士/游侠/收获者/血族/铁卫）+ 五行门派 4（焚天祭司/青囊药王/太白剑客/沧海鲛人）+ 进阶 2（弹雨枪手/百战军侯）。每人 1 套 `stats` + 1 个专属 `trait` + 1 个主动技能 |
| 武器 | **28**（内置） | 18 常规可掉落/可购买 + 4 状态流（震雷法锣/沥青长鞭/赤焰折扇/青藤缠索）+ 4 高品阶（火焰长剑/混沌重锤/磁轨炮/疫病连弩）+ 霜牙重锤/鎏金权杖 + **6 个进化形态**（双管神射/蜂巢风暴/毁灭炮/斩魄刀/加特林/榴弹炮，不进商店池、波末合成） |
| 道具 | **52** | 泛用属性 + 状态附魔（on_hit_*）+ 武器向（斩击/射程/弹速/爆炸范围）+ 角色向 + 前期过渡（common 小件）+ 后期神品（legendary） |
| 法宝 | **18** | 5 行各 2~4 件；6 种 trigger，每种限 1 件 |
| 升级（升级池） | **31** | 泛用属性 + 状态流 + 武器向 + 角色向（effects 集中在单一标签族，靠 affinity 推导形成角色专属池） |
| 状态 | **6** | 燃烧 / 中毒 / 流血 / 冰冻 / 减速 / 眩晕，全部映射到五行 |
| 五行反应 | **10** | 相生（generate）5 + 相克（overcome）5 |
| 敌人 | **21 普通 + 6 BOSS** | 基础 10 + 五行阵营 10 + 示例 mod 1；BOSS 池 6 个轮换 |
| 奇遇事件卡 | **10** | 按关卡主题分布（幽篁竹林 / 荒古废庙 / 幽冥鬼域） |
| 地图主题 | **3** | 幽篁竹林（W1-3）/ 荒古废庙（W4-6）/ 幽冥鬼域（W7-10），无尽循环 |
| 成就 | **12** | 全部由跨局统计计数器实时推导 |
| 角色印记（SIGIL） | **11** | 4 种绘制原语：spark / mote / edge / ring |
| 难度 | **3 + 无尽** | 普通 / 困难 / 噩梦 + 独立「无尽炼狱」模式 |
| 图鉴分类 | **9** | 状态 / 五行 / 武器 / 道具 / 法宝 / 升级 / 敌人 / 奇遇 / 成就 |
| autoload 单例 | **16** | Config / GameRng / EventBus / GameState / Registry / Settings / Haptics / Sfx / Music / CodexData / PlatformAchievements / SaveRun / Leaderboard / MetaProgress / Unlocks / **RunRules** |

> ⚠ 历史文档曾写「18 角色」「20 波」，均为早期规划值。**当前真值：12 角色、10 波**
> （`Config.WAVES_TOTAL := 10`，`Config.BOSS_WAVE := 10`）。想扩量改常量 + 补 `Registry` 内容即可，
> 冒烟测试的 `_check_phase3_content()` 会守住下限。

### 原型 → Godot 移植清单
- [x] CONFIG 数值表（玩家/武器/敌人/波次/升级/道具）→ `scripts/core/config.gd`
- [x] 可复现随机数（mulberry32 + seed）→ `scripts/core/rng.gd`
- [x] 玩家移动手感（加速度 + 阻尼）→ `scenes/characters/player.tscn`
- [x] 输入映射（WASD/方向键 + 手柄左摇杆/十字键 + 鼠标点触移动，暂停键）
- [x] 主场景骨架（竞技场边框 + 相机跟随 + 暂停）
- [x] 武器系统（5 把武器自动攻击 + 暴击/飘字/粒子/震屏，冒烟测试通过 2026-08-31）
- [x] 敌人 AI（5 种 + BOSS：射手风筝+侧移射击、BOSS 环形弹幕+半血狂暴、runner 抖动、敌间分离、敌方子弹、flee 退场；冒烟测试通过 2026-08-31）
- [x] 波次管理（10 波曲线+刷怪权重组合+出怪圈+波间休整+BOSS 波不死不休+通关/败北结算，冒烟测试通过 2026-08-31）
- [x] 掉落拾取（经验/材料/红心磁吸，冒烟测试通过 2026-08-31）
- [x] 升级三选一 UI（键盘 1/2/3 + 手柄焦点 + 鼠标，冒烟测试通过 2026-08-31）
- [x] 商店系统（购买/刷新/回血/商品锁定，波末清场自动进入；全代码构建 UI + 手柄焦点，冒烟测试通过 2026-09-02）
- [x] HUD（血条/经验条/武器槽/属性面板，冒烟测试通过 2026-09-02）
- [x] 手柄 UI 导航 + 震动反馈（Haptics autoload 节流震动 + 升级/商店焦点导航 + 插拔提示，冒烟测试通过 2026-09-02）
- [x] 主菜单系统（难度/角色/初始武器/初始道具选择，选项全部来自 Registry 注册表，2026-09-02）
- [x] 创意工坊架构（Registry autoload 注册表 + JSON manifest mod 加载 + 工坊浏览/编辑器 UI + 示例 mod；武器/道具/升级/敌人/难度/角色全数据驱动，编辑器保存持久化到 user://mods，敌人 AI 类型化（chaser/runner/shooter/boss），boss/spawn_table 可 mod 覆盖，2026-09-02）
- [x] 设置系统（主菜单入口；分辨率/窗口·无边框·全屏/帧率上限/主音量·音效·音乐独立音量/按键即时重绑定/手柄按键说明，Settings autoload 持久化 user://settings.cfg；修复手柄 A 不能确认：显式绑定 ui_accept=Enter/Space/A、ui_cancel=Esc/B；stretch aspect=keep 防画面裁切，2026-09-03）
- [x] 粒子/震屏/飘字特效（Burst 粒子池 + FloatingText 伤害/闪避/回血飘字 + Explosion 扩散光环+橙粒子 + Slash 近战弧光 + 受击闪白/血条 + BOSS 死亡大爆发/震屏 14，fx 组冒烟断言，2026-09-02）
- [x] 音效池走 SFX 总线；8 首 BGM 走 Music 总线（外部 WAV 素材 assets/audio/bgm/ 优先，缺失时回退程序生成；战斗按波次 1-4/5-7/8+ 三档 + BOSS + 胜利/失败结算曲；切曲淡出→换曲→淡入，波次 0.35s / 商店 0.4s / 结算 0.6s）；设置页主/音效/音乐独立音量 + 静音（2026-09-09）
- [x] 移动端虚拟摇杆 + 触屏暂停按钮；桌面鼠标点触移动（单击走向点击点、长按拖动跟随光标轨迹，到达自动停下，2026-09-09）
- [x] 自定义开局规则（RunRules autoload：11 项规则逐条可调 —— 敌人血量/伤害/刷怪密度/精英概率、波次总数/时长、武器槽/材料掉落/商店价格、禁用刷新/禁用回血；参数合成 "custom" 难度条目注入 Registry，战斗逻辑零改动生效；**方案 A：规则难度组是倍率，继承向导第 4 步所选基准难度叠加**（`base_difficulty_id` 随存档往返，修掉"选噩梦却只改波次被静默降级"的旧缺陷），首页可选基准难度 + 实算预览「最终 → 血×… 伤×… 密×… 精英…%」；难度评级改为相对基准计算；预设保存/载入/删除；挑战码 `BTL2-<基准>|…` 携带基准难度、兼容 v1 旧码；规则参数随局内存档保存、读档重建；自定义局不入排行榜与每日挑战；冒烟测试 12+ 项断言通过，2026-09-11）

### 数值调整记录（2026-09-02，用户确认）
- 当前初始移速 742（配置唯一源：`Config.PLAYER.base_speed`）
- 掉落物磁吸力度 ×2（900 → 1800）
- 波次时长：第 1 波 45s，每波 +5s（原 20+1.5w）
- 波末清场只清敌人与敌方子弹，掉落物保留原位跨波（不再全场回收）

> 里程碑顺序建议：先把移植清单清完（= Godot 版 Phase 1+2），再按下方 Phase 3/4 填内容、做移动端。

---

## 一、游戏定位与核心设计

### 1.1 一句话定义
俯视角竞技场生存射击 + Roguelite Build 构筑：玩家只控制走位，角色自动攻击，在一波波敌潮中存活并不断变强。

### 1.2 核心循环（Core Loop）
```
主菜单 → 开局向导（难度 / 角色 / 初始武器 / 初始道具，按构筑亲和排序）
→ 进入竞技场 → 走位躲避 + 自动攻击 → 击杀敌人掉落经验/材料
→ 升级选强化（3 张，保底 1 张契合当前构筑）
→ 波次结束 → 商店（武器/道具/升级/法宝，1 格保底契合项）
→ 商店关闭后可能触发奇遇事件卡（30% 概率，每局 3~5 次）
→ 下一波（难度递增、每 3 波换地图主题、每 4 波触发事件波）
→ 第 10 波 BOSS → 胜利/死亡 → 结算（精华/解锁/成就）→ 天赋树花精华 → 再来一局
```

### 1.3 关键设计要素
| 要素 | 说明 |
|------|------|
| **操作极简** | 只需要移动（WASD/方向键/摇杆/鼠标点触），攻击全自动，降低上手门槛 |
| **Build 深度** | 6 个武器槽（军火专家天赋可 +1）+ 攻击/状态/经济三类属性 + 五行反应 + 法宝 trigger，每局体验不同 |
| **10 波节奏** | 每波 45s 起、每波 +5s（`Config.wave_duration`），波间商店，单局约 20-25 分钟（无尽则可无限续） |
| **角色差异化** | 12 角色，各有独立初始属性 + 专属特性（stats/aura/thorns/momentum 四类）+ 主动技能 |
| **随机关卡** | 3 套主题轮换，每波随机生成 30-50 块障碍物（BOSS 波压到 15 以内），障碍物阻挡子弹与移动 |
| **五行反应** | 6 状态映射 5 行，两行相遇触发 5 相生（增强）+ 5 相克（爆发）；相克有 12× 伤害护栏 |
| **法宝构筑** | 6 种触发时机（反应/击杀/施加状态/造成伤害/受击/波末），每种限 1 件，价格高于道具 |
| **永久成长** | 死亡后保留精华 → 通用天赋 5 系 + 门派专属天赋 5 门；角色/武器由累计统计实时解锁 |
| **目标平台** | PC（Windows 优先）+ 移动端 + 手柄（已确认，见第〇节） |

### 1.4 你的差异化方向（已落地部分）
- 换皮主题：**中式玄幻**（五行门派、法宝、水墨关卡、竹叶/香烟/鬼火氛围粒子）—— 已实现
- 新增机制：**五行相生相克反应系统**、**法宝触发式特效**、**同族武器共鸣**、**武器进化树**、**奇遇事件卡**、**门派天赋树** —— 已实现
- 新增机制（候选）：宠物协同、多人合作（本地/在线联机）、可破坏场景
- 关卡互动：障碍物已阻挡子弹/移动 + 事件波（宝箱守卫 / 精英狩猎 / 流星雨）—— 部分落地

---

## 二、技术选型

### 2.1 游戏引擎对比

| 维度 | **Godot 4.x**（推荐） | Unity 2022/6 |
|------|----------------------|---------------|
| **原型匹配** | Brotato 原生引擎，2D 渲染管线专为 2D 设计 | 2D 功能完整但引擎较重 |
| **语言** | GDScript（类 Python，极快上手）/ C# | C# |
| **授权** | MIT 完全免费，无分成无门槛 | 收入超阈值需付费订阅 |
| **体积** | 轻量（~100MB），启动快 | 较重（数 GB），对低配机不友好 |
| **2D 工具** | 内置 TileMap、动画编辑器、节点树架构 | 需配合 Asset Store 插件 |
| **生态** | 社区快速增长，教程丰富，Brotato 有大量拆解参考 | 生态最成熟，资源/插件最多 |
| **跨平台** | PC/移动/Web 均可，主机需第三方移植 | 全平台原生支持，主机工具链成熟 |
| **学习成本** | 1-2 个月基础熟练 | 2-4 个月基础熟练 |

**结论（已确认）：采用 Godot 4.7.2-stable**，渲染后端 `gl_compatibility`（最大兼容性，便于移动端与低配机）。目标平台 PC + 移动端 + 手柄与 Godot 的导出能力和 Input Map 手柄映射完全匹配。

> 构建期环境（可复现）：Godot 4.7.2-stable win64 已就位；冒烟测试运行器 `game/tools/run_smoke.ps1`，
> 默认引擎路径可通过参数覆盖（脚本内默认指向本机临时目录）。**该路径是环境细节，换机需同步改。**

### 2.2 开发语言
- **GDScript**：Godot 首选，语法简洁，热重载友好，90% 的游戏逻辑用它写
- **C#**：Godot 也支持，适合有 C# 背景或需要高性能的复杂系统
- **GDExtension（C++）**：仅在性能瓶颈时用于核心算法（如大量子弹物理），初期不需要

### 2.3 配套工具链

| 类别 | 工具 | 用途 |
|------|------|------|
| **版本控制** | Git + GitHub/Gitee | 代码版本管理，必配 |
| **项目管理** | Trello / Notion / 飞书 | 任务看板、需求池、Bug 追踪 |
| **像素美术** | Aseprite / Pyxel Edit | 像素画角色、道具、特效 |
| **矢量/UI** | Figma / Affinity Designer | UI 界面、图标、矢量素材 |
| **动画** | Godot 内置 AnimationPlayer / Spine | 角色动画、特效序列 |
| **音频** | BFXR / LabChirp（音效）+ Bosca Ceoil / LMMS（BGM） | 独立开发者免费音频方案 |
| **关卡/数据** | Godot 内置 + JSON/Tres 资源 | 武器/道具/敌人数值配置 |
| **性能分析** | Godot Profiler / Debugger | 帧率、Draw Call、内存分析 |
| **视频/演示** | OBS | 开发日志录制、预告片素材 |

### 2.4 技术架构要点（已落地）

---

## 三、核心系统拆解

### 3.1 系统总览（含实现状态的层次图）
```
┌───────────────────────────────────────────────────────────┐
│                      框架层（autoload）                     │
│  Registry 内容注册表 │ Config 数值源 │ EventBus 事件总线      │
│  ObjectPool 对象池 │ GameState 局内状态 │ SaveRun 局内存档   │
├───────────────────────────────────────────────────────────┤
│                      战斗层（systems / scripts）             │
│  玩家控制+技能 │ 武器/进化/共鸣 │ 子弹与投射物 │ 4 类敌人 AI  │
│  伤害与属性 │ 6 种状态 │ 10 种五行反应 │ Combat 空间索引     │
│  Obstacles 解析几何 │ ArtifactSystem 法宝触发                │
├───────────────────────────────────────────────────────────┤
│                      成长层                                 │
│  经验升级(3选1+亲和) │ 商店 │ 道具 │ 法宝 │ 奇遇事件卡        │
│  波次管理(10波+事件波) │ BOSS 战 │ 难度+无尽 │ 开局向导       │
├───────────────────────────────────────────────────────────┤
│                      局外层（跨局持久化）                    │
│  CodexData 图鉴/统计 │ Unlocks 解锁推导 │ MetaProgress 天赋   │
│  Leaderboard 积分榜 │ PlatformAchievements 平台桥            │
├───────────────────────────────────────────────────────────┤
│                      表现层                                 │
│  HUD/菜单/图鉴/商店 UI │ 粒子与飘字 │ 震屏与顿帧 │ 印记      │
│  地图主题(3套) │ 掉落物 │ 摄像机跟随 │ 音效/BGM             │
└───────────────────────────────────────────────────────────┘
```

### 3.2 各系统详细说明

> 本节按**代码现状**描述（不再写「计划做」的时态）。文件路径均为 `game/scripts/` 下相对路径。

#### 3.2.1 角色控制系统（`characters/player.gd`）
- 输入：WASD/方向键 / 手柄左摇杆 / 移动端虚拟摇杆（`ui/touch_controls.gd`）/ 鼠标点触移动（`ui/mouse_move.gd`：单击走位、长按拖动跟随光标轨迹，键盘输入即时接管）
- 移动：Top-down 八方向，加速度 + 阻尼手感；世界边界 1600×1600
- 自动瞄准：自动锁定最近敌人，障碍物做视线遮挡（打不到墙后的敌人）
- 主动技能：每角色 1 个，独立 CD（12-18s），5 种 kind —— `buff` / `burst_damage` / `nova_status` / `self_heal` / `grant_materials`
- 角色属性：生命、速度、伤害、攻速、暴击率/暴伤、闪避、护甲（递减减伤）、拾取范围、收获、生命回复、吸血、状态命中/伤害/持续/扩散、6 种 `on_hit_*` 触发、4 种武器行为加成（斩击范围/弹丸射程/弹速/爆炸范围）、残血增伤、战意叠层

#### 3.2.2 武器系统（`core/config.gd` WEAPONS + `characters/player.gd`）
- 武器槽 6 个（`Config.WEAPON_SLOTS`），军火专家天赋可 +1
- 攻击类型：`projectile`（弹丸/散射/溅射）与 `melee`（弧形挥砍，带 `range` + `swing_arc`）
- 武器数值全字段驱动：cd / dmg / bspeed / pellets / arc / spread / splash / bullet_life / shake / status / status_chance / status_stacks
- **进化树**：基础武器持有 3 把（`evolve_need`）后，波末可在 `evolve_branches` 中选择进化；
  手枪→{冲锋枪, 霰弹枪, 双管神射}、砍刀→{太刀, 毒牙匕首, 沥青长鞭, 斩魄刀}、冲锋枪→{蜂巢风暴, 加特林}、霰弹枪→{毁灭炮, 榴弹炮}、太刀→{斩魄刀, 火焰长剑}
- **同族共鸣**（`WEAPON_FAMILY_SYNERGY`）：持有同 `family`（gun / blade / element / heavy）≥2 把时，每多一把叠一层加成
- **武器外观族**（`Registry.WEAPON_FX`，12 种）：bolt / pellet / flame / rocket / lance / frost / thunder / vine / bell / slash / whip / smash，配色随 status 走（同族也能一眼区分元素）
- **角色印记**（`Config.SIGILS`）：11 个印记 × 4 种绘制原语（spark/mote/edge/ring），叠加在弹丸/刀光/爆炸上；「这是谁在用」与外观族的「这是什么武器」正交

#### 3.2.3 子弹与投射物系统（`weapons/bullet.gd` / `enemy_bullet.gd`）
- 对象池管理（同屏可能上百发）
- 弹道：直线为主，支持散射（pellets + arc）、溅射（splash 触发 `explosion.gd`）、穿透、近战弧光（`slash.gd`）
- 命中判定：圆形碰撞盒 + `Combat` 空间网格；与障碍物用线段-圆解析求交（`Obstacles.first_block_t`，未命中返回 `INF`）
- 敌方子弹独立池化，撞墙不做特效（刻意，省性能）

#### 3.2.4 敌人与 AI 系统（`enemies/enemy.gd`）
- 敌人类型：21 普通 + 6 BOSS。AI 分四类 —— `chaser` 追击 / `runner` 抖动冲刺 / `shooter` 风筝射击 / `boss` 环形弹幕
- 基础 10：追击者 / 冲锋者 / 坦克 / 射手 / 蜂群 / 自爆者 / 巫师 / 暗影 / 守卫 / 宝箱守卫
- 五行阵营 10：火鬼/火萨满、木灵/藤兽、金俑/刀僧、水仙/冰巫、土傀儡/石巨
- 敌间分离（避免叠一起）、`status_resist` 异常抗性、每敌独立 `heart_chance` 掉红心
- **BOSS 池 6 个轮换**（`Config.BOSS_POOL`）：`boss` / `boss_spiral` / `boss_summoner` / `boss_phoenix` / `boss_leviathan` / `boss_titan`，各带 `death_skill`（ring / double_ring / miasma / rebirth / shockwave）
- 高难度从 `ELITE_POOL` 混入精英（W4+ 生效，`elite_chance` 由难度决定）

#### 3.2.5 伤害与属性系统
- 伤害公式：基础伤害 × (1 + 伤害加成%) × 暴击倍率 × 敌人护甲减伤 × 状态易伤（冰冻 +25%）
- 属性分类：
  - 生存：最大生命、生命回复、护甲、闪避
  - 输出：伤害、攻速、暴击率、暴击伤害、吸血
  - 功能：移速、拾取范围、收获（材料加成）
  - 异常：状态命中率 / 伤害加成 / 持续时长 / 扩散、6 种 `on_hit_*`
  - 武器行为：斩击范围、弹丸射程、弹速、爆炸范围、残血增伤、战意叠层
- **状态效果**（`Config.STATUS`，6 种）：
  | 状态 | 五行 | 效果 | 层数上限 |
  |------|------|------|---------|
  | 燃烧 burn | 火 | 持续伤害（dot_scale 0.18），3s | 5 |
  | 中毒 poison | 木 | 按最大生命百分比掉血（0.8%/tick），4s | 5 |
  | 流血 bleed | 金 | 持续伤害（dot_scale 0.12），高频叠加快，4s | 6 |
  | 冰冻 freeze | 水 | 定身，受伤 +25%，1.1s | 1 |
  | 减速 slow | 水 | 移速 -45%，2.6s | 1 |
  | 眩晕 stun | 土 | 短暂无法行动，0.9s | 1 |
  首次触发有状态色粒子/飘字/扩散环 + 独立音效 + 震屏/45ms 顿帧，全局 180ms 节流
- **五行反应**（`Config.REACTIONS`，10 种，key = 两行按字母序拼接）：
  - 相生 generate（5，不消耗层数、温和增强）：木生火（燃烧+1层/延时50%）· 火生土（眩晕+0.5s/燃烧伤害×1.3）· 土生金（流血+2层/眩晕期流血必暴）· 金生水 · 水生木（中毒扩散半径100）
  - 相克 overcome（5，消耗层数、爆发伤害）：木克土（AOE半径100×2.0）· 土克水（血量<20% 直接处决）· 水克火·蒸汽爆炸（半径120×2.5）· 火克金（受伤+50%持续3s）· 金克木·败血症（DoT×2 持续4s）
  - 伤害护栏：单次相克爆发对单目标 ≤ 玩家单次命中伤害 × `REACTION_BURST_CAP_MULT`(12.0)
  - ⚠ 战斗逻辑必须走 `Registry.find_reaction()`，`Config.get_reaction` 看不到 mod 反应

#### 3.2.6 经验与升级系统（`ui/level_up_ui.gd`）
- 击杀掉落经验晶体，自动吸附（受拾取范围影响）
- 升级暂停游戏，从升级池（31 项）中抽 3 张选 1（`GameState.level_queue` 支持连升）
- **构筑亲和加权**：抽取按「与当前角色 + 当前武器是否一路」加权（每命中 1 个标签权重 +85%，最多计 3 个），
  货架标记 `✦ 契合当前构筑`
- **亲和保底**：只把最后一张替换成契合项 —— 保留其余随机性，避免 build 变窄

#### 3.2.7 商店系统（`ui/shop_ui.gd`）
- 每波结束后进入商店；货币 = 战斗中收集的材料
- 商品池：武器（42% 概率位 `WEAPON_SHOP_CHANCE`）/ 道具 / 升级（29% `SHOP_UPGRADE_CHANCE`）/ 法宝（25% `ARTIFACT_SHOP_CHANCE`）
- 可刷新（砍价大师天赋 -12%/级）、可锁定商品保留到下一波、可花 15 材料回血（`SHOP_HEAL_PRICE`）
- 保底同升级：最后一格必出契合当前构筑的选项

#### 3.2.8 道具系统（`core/config.gd` ITEMS，52 件）
- 被动道具，永久属性；effects 键即 `player.stats` 键
- 稀有度五档：common / rare / epic / mythic / legendary（含 `RARITY_BASE_WEIGHTS` 按进度加权）
- 分族：泛用属性 · 状态附魔（on_hit_burn/poison/bleed/freeze/slow/stun）· 武器向（斩击/射程/弹速/AOE）· 角色向（标签集中，亲和倍率可达 ×2.7~3.55）· 前期过渡（common 小件）· 后期神品（legendary）
- 部分道具带**代价**（如荆棘重铠 移速 -5%、轻羽披风 护甲 -2、凝神宝珠 伤害 -8%）—— 取舍而非纯增益

#### 3.2.9 角色系统（`core/registry.gd` `_register_builtin`）
- 12 角色，每个 = `stats` 差异 + 专属 `trait` + 主动 `skill`
- **特性 4 类**（`Registry.TRAIT_KINDS`，未知 kind 直接拒登）：
  - `stats`：开局一次性注入 effects（如太白剑客 斩击范围+35%/暴伤+80%）
  - `aura`：光环，按 interval 对范围内敌人施状态（血族血雾流血 / 焚天祭司焚天灼烧 / 青囊药王瘴气中毒 / 沧海鲛人寒潮减速）—— 带范围圈与触发脉冲可视化
  - `thorns`：荆棘反击（铁卫受击反伤周围 155）
  - `momentum`：战意（百战军侯每波按击杀叠增伤，最高 +50%，每波重置）
- **解锁条件**（`CHARACTER_UNLOCKS`）：6 个角色需累计统计达标（反应 10 次 / 状态 25 次 / 击杀 200 / 胜利 1 次…），条件实时推导、解锁即永久
- 门派代表：焚天祭司→焚天门、青囊药王→青囊谷、太白剑客→太白剑阁、沧海鲛人→沧海门、铁卫→厚土宗

#### 3.2.10 地图与竞技场（`core/config.gd` MAP_THEMES + `systems/obstacles.gd`）
- 1600×1600 方形竞技场，边界不可越过，相机跟随玩家
- **3 套主题**（按波次切换 + 无尽循环）：
  | 主题 | 波次 | 障碍物外观 | 氛围粒子 |
  |------|------|-----------|---------|
  | 幽篁竹林 | W1-3 | 竹子 | 竹叶 |
  | 荒古废庙 | W4-6 | 石柱 | 香烟 |
  | 幽冥鬼域 | W7-10 | 石碑 | 鬼火 |
- 障碍物 30-50 块（BOSS 波 ≤15），出生点 200 半径内禁放；阻挡子弹与移动（解析几何，非物理体）
- 背景色 + 网格 + 边框强调色随主题变化

#### 3.2.11 波次与难度管理（`systems/wave_manager.gd`）
- **共 10 波**（`WAVES_TOTAL := 10`），BOSS 在第 10 波（`BOSS_WAVE := 10`）
- 波次时长：第 1 波 45s，每波 +5s（`wave_duration`）；波内持续刷怪直到时间结束或清场
- 难度曲线：敌人数量/血量/伤害随波次增长（`wave_*` 系列函数，按原型 JS 内联曲线移植）
- **事件波**（`EVENT_WAVE_INTERVAL = 4`，从第 3 波起、跳过 BOSS 波）：
  - `treasure` 宝箱守卫波：清完必掉 1 件高品阶道具
  - `hunt` 精英狩猎波：波末存活精英越少奖励越多
  - `meteor` 流星雨波：天降流星（预警圈 0.9s 后砸落 AOE，伤害 22，半径 78）
- BOSS 波「不死不休」：BOSS 未死则持续；波末清场只清敌人与敌方子弹（掉落物保留原位跨波）
- **无尽炼狱模式**（`GameState.endless`）：波次无上限（护栏 9999），每 10 波一轮 BOSS，积分 = 击杀/BOSS 击破/清波奖励，进 `Leaderboard`

#### 3.2.12 奇遇事件卡（`ui/event_card.gd` + `Config.EVENT_CARDS`）
- 商店关闭后 30% 概率触发（`EVENT_CARD_CHANCE`），每局 3~5 次（MIN/MAX），本局不重复抽（10 张都能见到）
- 10 张卡按主题分布（竹海清泉 / 古刹老僧 / 幽冥鬼灯 / 剑冢遗藏 / 白狐讨封 / 铁臂武僧 / 鬼市交易 / 竹灵赐福 / 血月当空 / 龙门试炼）
- 卡牌提供**有代价的取舍**（如写入 `GameState.next_wave_elite` 增加下波精英换取收益）
- 复用 `Phase.SHOP` 流程（刻意，不新增阶段）

#### 3.2.13 UI 系统
- 主菜单（`ui/main_menu.gd`）：开始游戏 / 开局向导（难度→角色→武器→道具）/ 创意工坊 / 图鉴 / 天赋树 / 设置 / 每日挑战 / 无尽炼狱 / 排行榜 / 退出
- 战斗 HUD（`ui/hud.gd`）：血条、波次计时、材料数、等级/经验条、武器栏、属性面板、**异常状态图例**（图标/命中率/层数上限）、角色特性显示、技能 CD
- **图鉴**（`ui/codex.gd`，9 类）：状态 / 五行 / 武器 / 道具 / 法宝 / 升级 / 敌人 / 奇遇 / 成就。
  含来源与数值、mod 内容自动收录；名称搜索 + 全部/已解锁/未解锁筛选；未解锁灰态 + 解锁条件提示。
  解锁发精华（状态6/武器10/道具8/升级6/敌人8…），成就另发 10-200 精华；toast 可点击直达条目（局内自动暂停）
- 升级界面：3 张卡片（键盘 1/2/3 + 手柄焦点 + 鼠标）
- 商店界面：商品网格、购买/刷新/锁定
- 结算界面：本局统计、🍟 精华结算、🔓 解锁广播、成就达成
- 暂停菜单（含全状态图例）、进化选择（`evolve_choose.gd`）、设置页（`settings_ui.gd`）、创意工坊（`workshop.gd`）、调试面板（`dev_panel.gd`，带 Ctrl 守卫）

#### 3.2.14 存档与解锁系统
- 局内存档（`save_run.gd`）：暂停/退出时保存当前局状态，可续局（刻意不重放法宝，见「不修清单」）
- 永久存档（`user://` 下 JSON）：`codex.json`（图鉴+统计+成就）、`meta_progress.json`（精华+天赋）、`leaderboard.json`（积分榜）、`settings.cfg`（设置）
- **解锁系统**（`core/unlocks.gd`）：角色/武器开局门槛由 CodexData 累计统计实时推导，**无需单独持久化解锁态**；
  未解锁内容在主菜单灰态显示 🔒 + 条件提示；结算时一次性广播解锁 toast（持久化「已庆祝」防重复）；每日挑战可提前体验锁定角色
- **跨局成长**（`core/meta_progress.gd`）：土豆精华 → 通用天赋 5 系 × 3 级（生命/伤害/财运/砍价/军火）
  + 门派专属天赋 5 门 × 3 级（仅对对应门派角色生效）

#### 3.2.15 创意工坊（`core/registry.gd` + `ui/workshop.gd`）
- 所有内容经 `Registry` 注册，JSON manifest 可覆盖/新增：characters / weapons / items / upgrades / enemies / difficulties / reactions / artifacts
- mod 目录：`user://mods/<模组名>/manifest.json`，另扫描 `res://mods` 内置示例
- 每条内容有**逐字段范围校验**（数值区间、引用完整性），校验失败只 push_warning 并跳过该条，不影响其他内容
- 顶层可覆盖 `boss` 与 `spawn_table`（波次刷怪表）
- 游戏内编辑器可创建内容并持久化到 `user://mods/workshop_user/manifest.json`（带 .bak 轮转 + 原子写入）

#### 3.2.16 平台与成就桥（`core/platform_achievements.gd`）
- 12 项成就由跨局统计实时推导，达成发精华
- 接入 GodotSteam 后自动转发 Steam 成就；未接入时本地降级，成就 id 落盘 `user://steam_pending.json` 排队，检测到 SDK 后自动补发

---

## 四、开发里程碑（6-9 个月独立开发节奏）

### Phase 0：准备阶段（第 1-2 周）
- [ ] 确定游戏主题和差异化方向（换皮主题待定，机制先照原型走）
- [x] 完成核心设计文档（GDD）——本文档 + Web 原型即设计基线
- [x] 搭建 Godot 项目框架和目录结构（`game/`，2026-08-31）
- [ ] 配置 Git 仓库和 .gitignore
- [ ] 建立任务看板，拆分需求池

### Phase 1：核心玩法原型（第 3-6 周）
> 目标：一个能玩的最小闭环——移动、自动射击、敌人、存活
> ✅ 进度：Web 原型（index.html）已验证本阶段全部目标；Godot 版按第〇节「移植清单」推进
- [ ] 角色移动控制器（手感调优）
- [ ] 基础武器系统（1-2 把武器测试）
- [ ] 子弹对象池和碰撞
- [ ] 基础敌人 AI（追踪型）
- [ ] 伤害计算和血条
- [ ] 经验掉落和升级（3 选 1 强化）
- [ ] 简单竞技场地图
- [ ] 摄像机跟随
- [ ] **里程碑验证：能完整打一波敌人并升级**

### Phase 2：系统扩展（第 7-12 周）
> 目标：补齐核心系统，形成完整单局体验 —— **已完成**
- [x] 波次管理系统（10 波配置 + 事件波 + 无尽模式）
- [x] 商店系统（购买/刷新/锁定/回血）
- [x] 道具系统（已扩到 52 个道具）
- [x] 武器扩展（28 把内置，含 6 个进化形态）
- [x] 敌人扩展（21 普通 + 6 BOSS）
- [x] 角色系统（12 个可玩角色，各带特性 + 主动技能）
- [x] 属性系统完善（全属性接入，含 6 种 on_hit_* 与 4 种武器行为加成）
- [x] 状态效果（6 种状态 + 状态武器 + 异常道具 + 异常升级 + 五行反应）
- [x] BOSS 战（6 个 BOSS 轮换，各有 death_skill）
- [x] **里程碑验证：能完整打完 10 波通关**

### Phase 3：内容填充（第 13-20 周）
> 目标：足够的内容量支撑重复游玩 —— **已完成**
> 实测口径由冒烟测试 `_check_phase3_content()` 每次回归时断言
> （数量下限 + 新增 id 齐全 + 数据自洽 + Registry 与 Config 条数一致性），不再靠人工数数。
- [x] 角色扩充到 **12 个**（基础 6 + 五行门派 4 + 进阶 2；原目标区间 15-20 **未达标**）。
      已覆盖全部玩法原型（每种特性 kind 都有人用、每条 Build 路线都有代表角色）。
      扩量做法：在 `Registry._register_builtin` 加条目（`stats` + `trait` + `skill`）
      + 加进 `Config.DAILY_CHARACTERS`（否则新角色永不出现在每日挑战）
- [x] 角色专属特性（**12 个角色各 1 种**）：角色不绑定「初始武器」
      ——玩家在向导第 2 步改选武器时角色设定会被覆盖，绑定形同虚设。
      改为每人一种专属特性，数据驱动（Registry 校验 kind 白名单）：
      `stats` 7 个（弹速 / 射程 / 斩击范围 / 暴伤 / 残血增伤 / 经济…）、
      `aura` 光环 4 个（血雾流血 / 焚天灼烧 / 瘴气中毒 / 寒潮减速）、
      `thorns` 荆棘反击 1 个（铁卫受击反伤）、`momentum` 战意 1 个（百战军侯每波按击杀叠增伤）。
      主菜单角色卡与 HUD 属性区均显示特性，光环带范围圈与触发脉冲可视化
- [x] 武器扩充到 20-30 把（**26 把内置**：18 常规 + 4 进化形态 + 本次新增 4 把——
      磁轨炮/疫病连弩/霜牙重锤/鎏金权杖；另有示例 mod 武器 1 把）
- [x] 道具扩充到 30-40 个（**52 个内置**，已超出目标区间：原 28 + 上一批 8 个——
      守望徽章/猎人手记/磁极核心/荆棘重铠/轻羽披风/凝神宝珠/疫病瓶/日曜石；
      + 武器向 5 个——磨刀石/铳匠长管/霜核/高速膛线/毒囊；
      + 元素附魔向 5 个——火绒/毒腺/血瓶/霜片/雷杵；
      + 角色向 6 个——剑匣/校准仪/焚天符/霜心镜/怒血披风/聚宝囊。
      另有示例 mod 道具 1 个）
- [x] 构筑亲和（**抽取关联化**，2026-09-10）：商店商品、升级三选一、法宝掉落
      全部按「与当前角色 + 当前武器是否一路」加权。标签由数据自动推导
      （武器状态/形态/溅射 + 角色特性 effects + 已持有法宝五行 → `affinity_tags`；
      条目的 effects/element/params → `entry_tags`），命中越多权重越高（每标签 +85%，
      最多按 3 个计）。货架上以 `✦ 契合当前构筑` 标出，让玩家看得见为什么好。
      法宝 15 → 18（补 rare 档：燎原印/饮血齿/雾隐纱，覆盖燃烧/流血/减速三种既有法宝
      未覆盖的状态）
- [x] 角色印记（**SIGIL**，2026-09-10）：角色的元素 / 风格在武器表现上的专属痕迹 ——
      焚天祭司的弹丸拖着火星、沧海鲛人的挂着霜粒、太白剑客的刃光更亮、铁卫的弹丸带脉动环。
      与「武器外观族」正交：外观族回答「这是什么武器」，印记回答「这是谁在用」。
      11 个印记共用 4 种绘制原语（spark / mote / edge / ring），叠加在弹丸 / 刀光 / 爆炸上；
      推导优先取 `trait.sigil` 显式声明（空串 = 刻意无印记，如土豆勇者的均衡之道），
      否则按 kind → status → effects 推断。**刻意不依赖时间相位** ——
      静态外观的弹丸仍只画一次，加印记不引入逐帧重绘代价（压测 1979 → 1986ms）
- [x] 亲和保底（2026-09-10）：升级三选一与商店各保底 1 个契合项 ——
      单靠加权只是让契合项「更常出现」，玩家仍可能连着几级看不到任何与构筑有关的东西。
      保底把关联性从概率变成承诺。只替换最后一张 / 最后一格：保留其余随机性，
      避免每次都塞同一类卡（那会让 build 变窄，而不是变丰富）
- [x] 角色向内容池（**6 条升级 + 6 件道具**，2026-09-10）：effects 刻意集中在单一标签族
      （近战范围+暴击 / 弹速射程 / 燃烧异常 / 冰冻异常 / 残血增伤 / 经济拾取），
      使其对对应角色的亲和倍率天然高达 ×2.7~3.55，对无关角色则几乎不出现 ——
      **零机制改动**就形成了角色专属内容池（完全靠既有的 affinity 推导）
- [x] 武器向成长线（**4 条升级 + 5 件道具**，2026-09-10）：把「武器形态强化」做成
      可成长项，而不只是角色特性 —— 开刃（斩击范围）/ 加长枪管（弹丸射程）/
      高初速（弹速）/ 高爆装药（爆炸范围）；道具为同方向的强化版。
      对应 `melee_range_bonus` / `bullet_range_bonus` / `bullet_speed_bonus` /
      `aoe_radius_bonus` 四个属性键（此前只被角色特性使用）
- [x] 敌人扩充到 10-15 种 + 2-3 个 BOSS（**21 种普通敌人 + 6 个 BOSS**，已超出目标区间：
      基础 10 种 + 五行阵营 10 种 + 示例 mod 1 种；BOSS 池 6 个轮换）
- [x] 地图主题（2-3 个皮肤）（**3 个**：幽篁竹林 / 荒古废庙 / 幽冥鬼域，
      背景色 + 网格 + 边框强调色 + 专属障碍物外观 + 氛围粒子，按波次 1-3 / 4-6 / 7-10 切换）
- [x] 解锁系统：角色/武器的开局选择门槛，条件由 CodexData 累计统计实时推导
      （解锁即永久，无需单独解锁态存档）。6 个角色 + 12 把武器有解锁条件，基础内容与
      创意工坊 mod 默认已解锁；未解锁内容在主菜单灰态显示 🔒 + 条件提示；结算时一次性广播
      「🔓 解锁新内容」toast（Unlocks 单例持久化「已庆祝」防重复）。每日挑战可提前体验锁定角色
- [x] 门派专属天赋树（**2026-09-10**）：MetaProgress 新增 5 门派（焚天门/青囊谷/太白剑阁/沧海门/
      厚土宗）× 3 级，各堆对应门派的特色深度（状态伤害/扩散/斩击暴击/持续减速/生命护甲），
      **仅对对应角色生效**（开局按 character_id 判断），与全员生效的通用天赋互补；
      天赋树面板新增「门派专属」分区
- [x] 图鉴系统（**9 类**：状态/五行/武器/道具/法宝/升级/敌人/奇遇/成就，含来源与数值，mod 内容自动收录；搜索/解锁筛选/未解锁灰态）
- [x] 成就与统计（12 项成就 + 11 项跨局统计计数器，随 CodexData 持久化；图鉴统计总览/成就进度条 + 完成度图表 + 下一目标推荐；解锁/达成 toast 提示且点击直达图鉴条目；图鉴解锁与成就达成均发放土豆精华并支持旧档补发/防重复；PlatformAchievements 平台桥自动转发 Steam 成就，未接入时成就 id 落盘排队、检测到 SDK 后自动补发）
- [x] 难度选项（普通/困难/噩梦三档，`hp_mult`/`dmg_mult`/`spawn_mult`/`elite_chance` 四维缩放；
      另有独立的无尽炼狱模式——每 10 波 BOSS、波次无上限、积分排行榜）
- [x] 每日挑战（当日日期 FNV-1a 哈希决定种子/角色/难度/BOSS，全服同局；可提前体验未解锁角色）
- [x] **里程碑验证：内容量达标，Build 多样性可感知**
      （12 角色 × 28 武器 × 52 道具 × 18 法宝 × 31 升级 × 21 普通敌人 + 6 BOSS ×
      6 状态 × 10 五行反应 × 10 奇遇 × 3 主题 × 12 成就；`Registry` 全部数据驱动，创意工坊可继续扩量。
      注：Build 多样性的主观手感仍需封闭测试确认）

### Phase 4：打磨与优化（第 21-26 周）
> 目标：手感、性能、视觉表现达标
- [x] 全面手感调优（移动、攻击反馈、打击感——震屏/顿帧/角色印记/刀光扫过已大幅强化，2026-09-10）
- [x] 粒子特效（命中、爆炸、升级、死亡；另有五行反应粒子 + 3 种主题氛围粒子）
- [x] 屏幕震动和慢动作特效（震屏随爆炸/受击/BOSS 等事件触发；状态触发额外 45ms 顿帧强化打击感）
- [x] 手柄适配（左摇杆移动、UI 按键导航、震动反馈）
- [x] 移动端适配（虚拟摇杆、触控 UI、性能分级）
- [x] 性能优化（对象池已用于子弹/敌方子弹/掉落物，`Combat` 空间网格每帧重建，
      同屏敌人硬上限 240，特效各有存活上限；Draw Call 与 60fps 稳定性仍待实机压测确认）
- [x] UI/UX 打磨（界面美观度、操作流畅度）
- [x] 音效和 BGM 全部接入（8 首外部循环 BGM 素材：菜单/商店/战斗 1-4/战斗 5-7/战斗 8+/BOSS/胜利/失败，放 assets/audio/bgm/*.wav 即生效、缺失自动回退程序生成；game/tools/gen_bgm.py 可重新渲染；切曲淡入淡出；设置页独立音量）
- [ ] 平衡性调整（数值迭代，基于测试数据）—— 已有护栏（五行反应 12× 封顶、属性上下限校验），仍待测试数据迭代
- [x] 稳定性测试（冒烟测试 `game/tests/smoke_test.gd` 覆盖全系统，headless 回归）
- [ ] **里程碑验证：游戏体验流畅，无明显 Bug，性能达标**

### Phase 5：测试与发布（第 27-32 周）
- [ ] 内部测试（自己反复玩，记录问题）
- [ ] 封闭测试（找 10-20 个玩家试玩，收集反馈）
- [ ] 根据反馈调整平衡性和体验
- [ ] 准备商店素材（宣传图、预告片、描述文案）
- [ ] Steam 页面上线（Coming Soon）
- [ ] 发售前营销（社交媒体、Demo、试玩 Key）
- [ ] 正式发布
- [ ] 发布后热修和内容更新计划

---

## 五、美术资源规划

### 5.1 美术风格建议
- **像素风**：Brotato 本身是简约卡通风格，像素风制作成本低、风格统一、独立游戏接受度高
- **简约卡通风**：矢量/扁平风格，类似 Brotato 原版，制作快，辨识度高
- 建议：**16x16 或 32x32 像素角色 + 简洁 UI**，控制美术工作量

### 5.2 资源清单（对齐代码真值）

| 类别 | 实际数量 | 说明 |
|------|---------|------|
| **玩家角色** | 12 个 | 当前用代码绘制角色头像（`ui/character_avatar.gd`）+ emoji 图标，尚未接美术资源 |
| **敌人** | 21 种普通 | 现为几何图形（circle/square/diamond）+ 配色，待换美术 |
| **BOSS** | 6 个 | 较大尺寸，各有专属死亡技 |
| **武器图标** | 28 个（含 6 进化形态） | 现用 emoji，待替换 |
| **道具图标** | 52 个 | 现用 emoji，待替换 |
| **法宝图标** | 18 个 | 现用 emoji，待替换 |
| **子弹/投射物** | 12 种外观族 | bolt/pellet/flame/rocket/lance/frost/thunder/vine/bell/slash/whip/smash（代码绘制，可按 status 变色） |
| **地图瓦片** | 3 套主题 | 竹林/废庙/鬼域，含障碍物外观 + 氛围粒子 |
| **角色印记** | 11 个 × 4 原语 | spark/mote/edge/ring，代码绘制 |
| **UI 元素** | 全套 | 全部代码构建（无外部 UI 素材依赖） |
| **特效帧** | 全代码程序化 | Burst / FloatingText / SkillFx / Explosion / Slash / FlameJet / Meteor 等 |
| **宣传素材** | 待做 | 主视觉、商店头图、截图、GIF |

> **现状说明**：当前版本的美术表现**全部由代码程序化生成**（几何图形 + emoji + 粒子），
> 没有任何外部图片资源依赖。这是刻意选择 —— 先把玩法/手感/内容量做扎实，
> 美术替换留到 Phase 5 前统一进行。届时只需替换头像/敌人/图标，逻辑层零改动。

### 5.3 美术获取方式
- **自己画**：Aseprite 像素画，学习成本中等，独立开发者首选
- **免费素材**：itch.io、OpenGameArt、Kenney.nl 有大量免费可商用素材
- **购买素材包**：itch.io 上 $5-30 的像素素材包，性价比高
- **外包**：关键角色/宣传图可找画师外包，单价几百到几千元

---

## 六、音频资源规划

### 6.1 音效清单
- 角色移动、受击、死亡
- 各类武器射击音（区分武器类型）
- 子弹命中、爆炸
- 敌人死亡、精英出现
- 经验拾取、升级提示
- 商店购买、刷新
- UI 按钮点击
- 波次开始/结束提示
- BOSS 登场音效

### 6.2 BGM 规划
- 主菜单音乐（1 首，轻松氛围）
- 战斗音乐（2-3 首，紧张节奏感，可按波次阶段切换）
- BOSS 战音乐（1 首，更激烈）
- 商店音乐（1 首，舒缓）
- 胜利/失败结算音乐

> 已实现：8 首循环曲（menu / shop / battle / battle_mid / battle_late / boss / victory / defeat）以 16-bit PCM WAV 素材存放在 `game/assets/audio/bgm/`，Music 直接解析文件并循环播放；文件缺失或格式不支持时自动回退程序生成。战斗曲按波次 1-4 / 5-7 / 8+ 自动切换，胜利/死亡结算切 victory / defeat；切曲走淡出→换曲→淡入（波次 0.35s / 商店 0.4s / 结算 0.6s），避免硬切爆音；替换 BGM 只需覆盖同名文件（`game/tools/gen_bgm.py` 为素材生成器）。

### 6.3 音频工具
- **音效生成**：BFXR（免费）、LabChirp（免费）—— 8-bit 风格音效一键生成
- **BGM 创作**：Bosca Ceoil（免费）、LMMS（免费）、Soundtrap（在线）
- **音频编辑**：Audacity（免费）—— 剪辑、降噪、格式转换
- **免费音乐库**：Free Music Archive、Incompetech、Pixabay Music（注意授权协议）

---

## 七、数值与平衡性设计

### 7.1 核心数值框架
```
玩家成长 = 基础属性 + 武器伤害 × 数量 + 道具加成 + 升级强化 + 法宝触发 + 天赋/精华
敌人强度 = 基础值 × 波次系数 × 精英加成 × 难度倍率(hp/dmg/spawn)
经济系统 = 材料掉落 × 收获属性 → 商店消费 → 战力提升
反应爆发 = 状态强度(power) × 层数 × aoe_dmg_scale，单目标上限 = 玩家单次命中伤害 × 12
```

### 7.2 平衡设计原则（含已落地的护栏）

1. **每波都要有成长感**：每波结束进商店，至少能稳定买到 1 个契合构筑的选项（保底机制）
2. **Build 多样性**：6 条主要路线 —— 枪械速射流、近战斩击流、五行反应流、异常 Dot 流、残血狂战流、经济滚雪球流
3. **风险与收益**：高收益道具带明确代价（护甲/移速/伤害负值），奇遇卡提供「加下波精英换收益」的取舍
4. **难度曲线**：W1-2 教学期（低密度、单 AI）→ W3-6 成长期（引入事件波/精英/新主题）→ W7-9 挑战期（多 AI 混编）→ W10 BOSS
5. **避免无解局**：商店刷新 + 亲和保底 + 法宝/道具多源获取
6. **性能护栏**（Phase 5 补齐）：
   - 同屏敌人硬上限 `ENEMY_HARD_CAP = 240`
   - 特效存活上限：粒子 60 / 飘字 70 / 技能冲击波 14（`Config.FX_*_MAX_LIVE`）
   - 五行反应单目标伤害封顶 ×12（`REACTION_BURST_CAP_MULT`）
   - 空间网格查询上限：`MAX_QUERY_CELLS 4096` / `MAX_GRID_QUERY_RADIUS 4000`
7. **数据合法性护栏**：`Registry.STAT_LIMITS` / `EFFECT_LIMITS` 定义每个属性键的安全区间，
   mod 内容越界直接拒登（避免「配出暴击率 900%」这类溢出构筑）

### 7.3 数值调优方法
- 先用 Excel/Google Sheets 建数值模型，模拟成长曲线
- 游戏内埋点统计：各武器/道具胜率、平均通关波次、Build 分布
- 封闭测试收集玩家反馈，重点关注"太简单/太难/某个组合无敌"
- 迭代调整：每次调整幅度不要太大，观察数据变化

---

## 八、项目管理与协作

### 8.1 目录结构（实际）

```
game/                            # Godot 项目根
├── project.godot                # 16 个 autoload、输入映射、渲染设置
├── scenes/
│   ├── ui/                      # main_menu.tscn 等 UI 场景
│   ├── characters/              # player.tscn
│   └── …                        # 敌人/武器场景
├── scripts/
│   ├── core/                    # 核心框架
│   │   ├── config.gd            # ★ 唯一数值源（角色/武器/道具/敌人/反应/法宝/波次/主题）
│   │   ├── registry.gd          # ★ 内容注册表（mod 接口层 + 全字段校验）
│   │   ├── game_state.gd        # 局内状态（相位/无尽/每日/奇遇计数）
│   │   ├── event_bus.gd         # 事件总线
│   │   ├── object_pool.gd       # 对象池
│   │   ├── codex_data.gd        # 图鉴 + 统计 + 成就（持久化）
│   │   ├── meta_progress.gd     # 精华 + 天赋树（持久化）
│   │   ├── unlocks.gd           # 解锁推导
│   │   ├── leaderboard.gd       # 积分榜
│   │   ├── save_run.gd          # 局内存档
│   │   ├── settings.gd / haptics.gd / sfx.gd / music.gd
│   │   └── platform_achievements.gd   # Steam 桥
│   ├── systems/                 # 业务系统
│   │   ├── combat.gd            # 空间索引 + 线段-圆求交
│   │   ├── obstacles.gd         # 障碍物解析几何
│   │   ├── artifact_system.gd   # 法宝触发分派
│   │   └── wave_manager.gd      # 波次/事件波/BOSS
│   ├── characters/player.gd     # 玩家（移动/武器/技能/进化/共鸣）
│   ├── enemies/enemy.gd         # 敌人 AI（4 类）+ 状态执行 + 反应触发
│   ├── weapons/                 # bullet / enemy_bullet / slash / explosion
│   ├── fx/                      # burst / floating_text / skill_fx / 氛围粒子
│   ├── loot/loot.gd             # 掉落拾取
│   ├── ui/                      # 全部 UI（代码构建，无外部素材）
│   └── obstacle.gd
├── tests/smoke_test.gd          # ★ 冒烟测试（单文件，headless 回归）
├── tools/                       # run_smoke.ps1 / gen_bgm.py 等
├── mods/example_mod/            # 示例创意工坊 mod
└── assets/audio/bgm/            # 8 首 BGM（WAV）
```

> 与早期规划不同：**没有 `resources/` 目录** —— 数值全部在 `config.gd` 的常量表里，
> 不拆 Tres 文件。理由：单文件便于全局搜改与 diff，且 `Registry` 已提供 mod 覆盖能力。
> 场景文件也极少（主菜单 + 玩家），其余 UI 全代码构建。

### 8.2 架构约定与踩坑记录（重要）

这些是本项目已经付出代价换来的结论，改动前务必先读：

**GDScript / Godot 约束**

| 约定 | 原因 |
|------|------|
| **autoload 不能出现在 `const` 初始化器里** | `const X := Config.Y` 不可行 —— 那时 autoload 尚未就绪。需要常量就写成 `static func` 运行时读取（如 `Burst.max_live()`），static 函数访问 autoload 是合法的 |
| **`trait` 是 Godot 4.7 保留字** | 字段名只能用 `char_trait`；字典访问必须 `data["trait"]`（中括号），点号会解析失败 |
| **`Config.get_reaction` 不是生产接口** | 它只看内置表，看不到 mod 反应。战斗逻辑一律走 `Registry.find_reaction()`，否则 mod 反应静默失效 |
| **`is_finite(block_t)` 必须判** | `Obstacles.first_block_t` 未命中返回 `INF`，漏判会让全部子弹第一帧消失 |
| **护栏计数要能在「节点被外部销毁」时归零** | FX 的 `_live` 静态计数曾只在自己 `queue_free` 时递减；被 `remove_child` 提前收回时永不归零 → 上限永久偏高 → 特效逐渐消失且零报错。现统一走 `_exit_tree()` 兜底 |
| **自定义规则的难度走「合成难度条目」，不要新开战斗分支** | 难度全项目只有 3 个消费点（`enemy.gd` 的 hp/dmg、`wave_manager.gd` 的 spawn/elite），都走 `Registry.get_difficulty(id)`。把规则算成一个 `"custom"` 条目注入 `Registry.difficulties` 即可全量生效，无需改动任何战斗逻辑 |
| **`"custom"` 难度条目不落盘，读档必须先重建** | `Registry` 启动时只有内置难度。存档里 `difficulty_id = "custom"` 时，`SaveRun.restore` 必须先 `RunRules.apply_from_save()` 再校验难度合法性，否则会静默回落 `normal`（玩家的自定义局被悄悄改档） |
| **自定义规则与向导难度是「继承 + 叠加」，不是「替代」（方案 A）** | 规则里难度组四项（血量/伤害/密度/精英）是**倍率**，叠乘在向导第 4 步所选基准难度之上（`base_difficulty_id`，随存档往返）。曾有个缺陷：旧实现无论选什么难度都从 1.0 起算，导致「选噩梦 + 只改波次」被静默降级成普通。合成条目 `hp_mult = 基准.hp_mult × 规则倍率`；**精英概率是加成语义**（基准 + 增量）而非倍率，叠加后须 `clampf(…, 0, 1)`（校验器要求 `elite_chance ∈ [0,1]`） |
| **挑战码 v2 必须携带基准难度（`BTL2-`），v1 旧码显式回落 normal** | `BTL2-<基准下标>\|<规则字段…>`；「噩梦 + 1.5 倍血」与「普通 + 1.5 倍血」是两局完全不同的游戏，码里不带基准会被分享方静默复现成另一局。`decode()` 遇到 v1 码（无基准字段）时**必须显式重置为 normal** —— 否则会继承调用方残留的 base |
| **`MetaProgress.weapon_slots()` 是武器槽的唯一真值来源** | 全项目 11 处调用它。规则对槽位的覆盖做进这个函数内部（转发给 `RunRules.weapon_slots_total()`），11 处调用点自动感知，避免逐点改动漏一处 |
| **开局向导的网格列数必须自适应，不要写死** | 写死列数会在内容少时四周留白（武器步 6 张 ÷ 3 列只占 674/1000 面板宽）。统一走 `main_menu._fit_grid(n, min_h)`：按张数反推列数（判据：空槽最少 → 行数最少 → 卡片最宽，带宽度上限 500 与宽高比 1.9 护栏），再把卡片撑到该列宽、行高撑满；卡内元素随卡宽缩放。改每步张数（如 `LOADOUT_WEAPONS`）后无需手动调列数 |
| **向导卡面文字必须有硬护栏** | 卡片是固定尺寸网格单元。内容高度超过卡高时，居中 VBox 会**上下同时溢出到邻卡**。描述 Label 必须 `clip_text = true` + `size_flags_vertical = EXPAND_FILL`；文字最长的步骤（角色）单独传更大的 `min_h` |
| **武器数值单表真值：`price` / `shop_weight` 内联在 `Config.WEAPONS` 里** | 曾经 `WEAPONS` / `WEAPON_PRICES` / `WEAPON_SHOP_WEIGHTS` 三表逐键同步，注册期 `Config.WEAPON_PRICES[id]` 裸下标，缺 key 直接崩。2026-09-11 合并后：加武器只改 `WEAPONS` 一处；mod 武器缺字段由 `register_weapon._apply_defaults` 兜底（price 30 / shop_weight 1.0），不再崩。`WEAPON_FX` 保持独立（外观族与数值分离是刻意的） |
| **远程武器瞄准必须带弹道提前量** | `player.try_fire` 直接瞄敌人当前位置，低速弹丸（火焰 300 / 玄冰 320）打高速敌人永远空枪。现在走 `_lead_target()` 一元二次解析解（交会点），**不要用迭代法** —— 迭代在「慢弹丸 + 快目标」时来回震荡不收敛。近战与火焰喷射锥不需要预判（覆盖整段不依赖单点）。敌方速度由 `_enemy_vel()` 从「朝玩家方向 × speed」推算（敌人无 velocity 字段，位置是直接累加的），由冒烟 `_check_ballistic_lead()` 以解析解守护 |

**刻意的设计选择（不是缺陷，勿「修」）**

| 选择 | 理由 |
|------|------|
| 不用 Godot 物理引擎，`Obstacles` 用纯解析几何 | 同屏上百实体时物理体开销过大；解析求交可预测且更快 |
| `ObjectPool.release` 用 `remove_child` 而非 `queue_free` | 节点要复用，`queue_free` 会真正销毁 |
| `enemy_bullet` 撞墙不做特效 | 敌方弹幕量大，特效会淹没画面并拖垮帧率 |
| 奇遇卡复用 `Phase.SHOP` 流程 | 阶段状态机不新增分支，减少 UI/存档的耦合面 |
| `SaveRun.restore` 不重放法宝效果 | 法宝是触发式的，重放会产生重复结算；改由持有表恢复 |
| 调试后门带 Ctrl 守卫 | 防止误触影响正常游玩 |

**两条线段-圆求交实现并存**（`Combat.segment_circle_entry_t` 与 `Obstacles._segment_circle_entry_t`）
- 刻意不抽公共模块（避免 `class_name` 全局类缓存在 headless 下的加载顺序风险）
- 二者之间以「等价性契约」注释互指，并由冒烟测试 `_check_segment_math_equivalence()`
  以 400 组随机用例（固定种子 20260911）守住等价性

### 8.3 Git 规范
- 分支：`main`（稳定）、`dev`（开发）、`feature/xxx`（功能分支）
- 提交信息格式：`[模块] 简要描述`，如 `[武器] 新增散弹枪武器`
- `.gitignore` 排除：`.godot/`、`export/`、临时文件、冒烟测试输出（`smoke_out.txt` / `smoke_err.txt` / `run_game*.log`）
- 大资源文件考虑 Git LFS

### 8.4 测试与开发纪律
- **冒烟测试是安全网**：`game/tests/smoke_test.gd` 单文件覆盖全系统，改动后必跑。
  运行方式：`godot --headless --path game res://tests/smoke_test.tscn`
  （便捷脚本 `game/tools/run_smoke.ps1`，支持 `-Times N` 连跑）
  **判定看 stdout 是否出现 `SMOKE: PASS`，不要只看退出码。**
- 功能开发顺序：先跑通流程，再填内容，最后打磨
- 遇到技术难点先做最小验证原型，再整合到主项目
- 改动数值/内容后，同步更新 `_check_phase3_content()` 的下限断言

---

## 九、发布与运营

### 9.1 平台选择
| 平台 | 优先级 | 说明 |
|------|--------|------|
| **Steam（PC）** | 最高 | 独立游戏主阵地，Roguelike 玩家集中 |
| **移动端（iOS/Android）** | 高 | Brotato 移动端销量也很高，触控适配简单 |
| **Switch/PS/Xbox** | 中 | 需主机开发者资质，可后期移植或找发行商 |
| **Web（itch.io）** | 低 | 可发 Demo 引流，不作为主发布平台 |

### 9.2 Steam 发布准备
- 注册 Steamworks 开发者账号（$100 一次性费用/产品）
- 准备商店素材：
  - 主图标（胶囊图）
  - 宣传横幅
  - 游戏截图（5-10 张）
  - 预告片（1-2 分钟）
  - 游戏描述文案
- Steam 成就桥接层已完成（PlatformAchievements autoload：接入 GodotSteam 后自动转发 12 项成就；未接入时本地降级，成就 id 落盘排队到 user://steam_pending.json，下次启动检测到 SDK 后自动补发）；云存档/Steam 排行榜仍为可选后续
- 上架 Coming Soon 页面，至少提前 1-2 个月积累愿望单

### 9.3 营销建议
- **开发日志**：在 B 站/知乎/小红书/IndieNova 发布开发过程，积累关注
- **Demo 试玩**：Steam 新品节、itch.io 发布 Demo，收集反馈
- **KOL 合作**：找游戏区 UP 主/主播试玩，Roguelike 品类主播接受度高
- **社区运营**：建 Discord/QQ 群，和核心玩家保持沟通
- **发售节奏**：避开 3A 大作发售期，选独立游戏相对空档的时间

### 9.4 定价参考
- Brotato 原版定价 $4.99（约 ¥30-40）
- 独立 Roguelike 常见价位：¥30-60
- 建议：首发 ¥30-40 + 首发折扣 10-15%，降低购买门槛

---

## 十、风险与应对

| 风险 | 影响 | 应对策略 | 当前状态 |
|------|------|---------|---------|
| **内容量不足** | 重复游玩性差，差评 | 内容全部数据驱动 + `Registry` 注册，加条目即扩量；冒烟测试守住下限 | ✅ 已达标（12 角色 / 28 武器 / 52 道具 / 21+6 敌人 / 18 法宝 / 10 反应 / 10 奇遇），角色数仍可补 |
| **平衡性崩坏** | 某个 Build 无敌或完全无法通关 | 数据驱动配置方便快速调整；已有反应伤害封顶、属性区间校验；封闭测试重点验证 | ⚠ 护栏已就位，缺真实测试数据 |
| **性能问题** | 同屏敌人/子弹多时卡顿 | 对象池 + 空间网格 + 敌人硬上限 + 特效存活上限；用 Profiler 定位瓶颈 | ⚠ 机制已就位，60fps 稳定性待实机压测 |
| **美术风格不统一** | 视觉体验差 | 前期统一用代码程序化绘制（几何 + emoji + 粒子），规则天然一致；替换素材时按统一色板/尺寸规范 | ✅ 当前无美术风险（尚未引入外部素材）；换皮时需重定规范 |
| **开发周期失控** | 烂尾或无限延期 | 严格按里程碑推进；`smoke_test.gd` 保证每次改动可回归；"做完一个再做下一个" | ✅ 稳定推进中 |
| **Steam 曝光不足** | 销量惨淡 | 提前 3 个月做营销；参加 Steam 新品节；找主播试玩 | ⏳ 未开始（发布阶段任务） |
| **重构引入回归** | 改了 A 弄坏 B | 冒烟测试是唯一安全网：改完必跑，看 `SMOKE: PASS`；不为「优雅」做无收益重构 | ✅ 有测试兜底 |

---

## 十一、学习资源推荐

### Godot 学习
- **官方文档**：docs.godotengine.org（最权威，有中文）
- **B 站教程**："Godot 入门教程"系列，推荐关注"开发游戏的老王"等 UP
- **YouTube**：GDQuest、Clear Code、HeartBeast（英文，质量高）
- **书籍**：《Godot Engine 游戏开发实战》

### 游戏设计
- **《游戏设计艺术》**（Jesse Schell）—— 游戏设计圣经
- **《Blood, Sweat, and Pixels》**—— 独立游戏开发故事
- **GDC 演讲**：YouTube 上 GDC 频道，Roguelike 设计相关演讲
- **Brotato 拆解**：搜索"Brotato 游戏拆解"，有大量分析文章和视频

### 像素美术
- **B 站**："像素画入门"教程
- **网站**：Pixel Art Academy、Lospec（色板工具）
- **参考**：观察 Brotato、《吸血鬼幸存者》、《以撒的结合》等同类游戏的美术

---

## 十二、下一步行动清单

> 项目已远超「第一周原型」阶段，此处列的是**当前真正待办**。

### P0 —— 收敛与验证
1. **补齐角色数**：12 → 15+（在 `Registry._register_builtin` 加条目，各带 `stats` + `trait` + `skill`；
   同步加进 `Config.DAILY_CHARACTERS` 否则新角色永不出现在每日挑战，冒烟测试会断言池子覆盖）
2. **实机压测**：把波次拉到 15+、怪物密度拉满，用 Godot Profiler 看 Draw Call / 帧时间，
   确认 240 敌人上限下能否稳定 60fps
3. **封闭测试**：找 10-20 人试玩，重点收集「哪个 Build 无敌 / 哪一波劝退」，
   据此迭代 `Config.WAVE_*` 曲线与 `REACTION_BURST_CAP_MULT`
4. **补美术**：替换 emoji 图标为正式像素/矢量素材（角色头像 → 敌人 → 武器/道具/法宝图标）

### P1 —— 内容与体验
5. **扩奇遇卡 / 事件波种类**（当前各 10 / 3 种，重复度高）
6. **新增 Build 路线**：目前 6 条主流已覆盖，可考虑召唤流 / 光环流
7. **补全状态交互**：目前 6 状态两两组合只用了 10 种，可考虑更多反应类型

### P2 —— 发布准备
8. Steam 页面上线（Coming Soon）+ 商店素材（胶囊图 / 截图 / 预告片）
9. 开发日志连载（B 站 / 知乎 / IndieNova）
10. Demo 制作（Steam 新品节用）

---

> **最后建议**：类土豆兄弟游戏的核心竞争力不是技术，而是**手感 + Build 深度 + 内容量**。
> 本项目已经在这三条上都有了扎实的地基 —— 全数据驱动的内容层、10 种五行反应带来的构筑深度、
> 以及一个覆盖全系统的冒烟测试安全网。接下来最关键的是**别停下跑测试**，
> 以及**拿真实玩家数据去调数值**，而不是靠感觉调。
